% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perspective.R
\name{perspective}
\alias{perspective}
\title{Create a Perspective Interactive Viewer}
\usage{
perspective(
  data,
  columns = NULL,
  group_by = NULL,
  split_by = NULL,
  sort = NULL,
  filter = NULL,
  expressions = NULL,
  aggregates = NULL,
  plugin = NULL,
  plugin_config = NULL,
  theme = "Pro Light",
  settings = TRUE,
  title = NULL,
  editable = FALSE,
  use_arrow = FALSE,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data.frame or matrix to display.}

\item{columns}{Character vector of column names to show. If \code{NULL},
all columns are shown.}

\item{group_by}{Character vector of column names to group rows by (row pivots).}

\item{split_by}{Character vector of column names to split columns by (column pivots).}

\item{sort}{A list of two-element vectors, each containing a column name and
a direction (\code{"asc"}, \code{"desc"}, \code{"col asc"}, \code{"col desc"},
\code{"asc abs"}, \code{"desc abs"}, \code{"col asc abs"}, \code{"col desc abs"}).
For example: \code{list(c("mpg", "desc"))}.}

\item{filter}{A list of three-element vectors, each containing a column name,
an operator (\code{"=="}, \code{"!="}, \code{">"}, \code{"<"}, \code{">="}, \code{"<="},
\code{"begins with"}, \code{"contains"}, \code{"ends with"}, \code{"in"}, \code{"not in"},
\code{"is null"}, \code{"is not null"}), and a value.
For example: \code{list(c("cyl", "==", "6"))}.}

\item{expressions}{Character vector of Perspective expression strings for
computed columns. For example: \code{c('"Profit" / "Sales"')}.}

\item{aggregates}{A named list mapping column names to aggregate functions.
For example: \code{list(mpg = "avg", hp = "sum")}.}

\item{plugin}{Character string specifying the visualization plugin.
Options include: \code{"Datagrid"}, \code{"Y Bar"}, \code{"X Bar"},
\code{"Y Line"}, \code{"Y Area"}, \code{"Y Scatter"}, \code{"XY Scatter"},
\code{"Treemap"}, \code{"Sunburst"}, \code{"Heatmap"}.}

\item{plugin_config}{A list of plugin-specific configuration options.}

\item{theme}{Character string specifying the CSS theme. Options:
\code{"Pro Light"}, \code{"Pro Dark"}, \code{"Monokai"},
\code{"Solarized Light"}, \code{"Solarized Dark"},
\code{"Vaporwave"}. Default is \code{"Pro Light"}.}

\item{settings}{Logical; whether to show the settings/configuration panel
sidebar. Default \code{TRUE}. This is the interactive UI where users
drag-and-drop columns, change chart types, add filters, etc.}

\item{title}{Character string for the viewer title. If \code{NULL}, no title
is shown.}

\item{editable}{Logical; whether the data in the grid is user-editable.
Default \code{FALSE}.}

\item{use_arrow}{Logical; if \code{TRUE}, serialize data using Arrow IPC
format (base64-encoded) for better performance with large datasets.
Requires the \code{arrow} package. Default \code{FALSE}.}

\item{width}{Widget width (CSS string or numeric pixels).}

\item{height}{Widget height (CSS string or numeric pixels).}

\item{elementId}{Optional explicit element ID for the widget.}
}
\value{
An htmlwidgets object that can be printed, included in R Markdown,
Quarto documents, or Shiny apps.
}
\description{
Creates an interactive pivot table and visualization widget powered by the
FINOS Perspective library. The viewer provides a self-service UI where users
can interactively change chart types, group/split/filter/sort data, create
computed columns, and configure aggregations.
}
\examples{
# Basic data grid
perspective(mtcars)

# Bar chart grouped by cylinder count
perspective(mtcars, group_by = "cyl", plugin = "Y Bar")

# Filtered and sorted view
perspective(iris,
  columns = c("Sepal.Length", "Sepal.Width", "Species"),
  filter = list(c("Species", "==", "setosa")),
  sort = list(c("Sepal.Length", "desc"))
)

}
